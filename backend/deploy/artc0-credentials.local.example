# Local only — copy to artc0-credentials.local (gitignored) after deploy.
# Host: artc0.f0xx.org:8090 → artc0.intra.raptor.org:8080
IMU_API_KEY=<from setup-only.sh output or /etc/esp32-imu-backend/env on artc0>

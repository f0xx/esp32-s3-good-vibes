<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to LAN backend VM during dev; use TLS in production. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
